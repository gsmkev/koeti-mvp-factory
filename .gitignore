# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
dist/
*.tsbuildinfo

# Env
.env
.env.local
.env*.local

# Turbo
.turbo/

# Python venv
.venv/

# Knowledge graphs (auto-generated)
graphify-out/
.code-review-graph/
.vercel

# Playwright MCP test artifacts
.playwright-mcp/
/*.png

# Local self-hosted PostHog stack (machine-local: vendored repo, GeoLite data,
# service creds — docker-compose.yml references these, so that change stays local too)
/posthog/
/share/
.env.services

# Local agent worktrees
.claude/worktrees/
